datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Seal {
  id         Int      @id @default(autoincrement())
  hashOutput String   @unique
  name       String
  intention  String
  createdAt  DateTime @default(now())
}